diff --git a/Makefile b/Makefile_new
index 5651161..1a88430 100644
--- a/Makefile
+++ b/Makefile_new
@@ -7,3 +7,8 @@ endif
 ifeq ($(subdir),csu)
 gen-as-const-headers += tlsdesc.sym
 endif
+
+# to pull in __aeabi_read_tp, needed for tls
+ifeq ($(subdir),malloc)
+$(objpfx)libmemusage.so: $(common-objpfx)libc_nonshared.a
+endif
